# We want to keep our version of the file when there is a conflict to make sure timing flags aren't removed
# Nothing to do: %A (the second parameter) already contains our version
# Just indicate the merge has been successfully "resolved" with the exit status
#***Note that this script must be somewhere in your $PATH
exit 0
